---
# Loki tuning for Raspberry Pi (ARM64)
# Optimizing for low memory footprint, single-node setup

deploymentMode: SingleBinary

loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1
  storage:
    type: filesystem
    filesystem:
      chunks_directory: /var/loki/chunks
      rules_directory: /var/loki/rules
  schemaConfig:
    configs:
      - from: "2024-01-01"
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: index_
          period: 24h

# Dedicated config for the single binary deployment
singleBinary:
  replicas: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 1024Mi
  persistence:
    enabled: true
    storageClass: "nfs-client"
    size: 50Gi

# Disable SimpleScalable components (Required for SingleBinary to pass validation)
read:
  replicas: 0
write:
  replicas: 0
backend:
  replicas: 0

# Disable resource-heavy distributed caching (not needed for simple Pi setups)
chunksCache:
  enabled: false
resultsCache:
  enabled: false

# Re-enable Canary but with tiny resources because the chart validates its presence
lokiCanary:
  enabled: true
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 50m
      memory: 64Mi

# Gateway provides HTTP endpoint (as used in Alloy config)
gateway:
  enabled: true
  replicas: 1
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi
