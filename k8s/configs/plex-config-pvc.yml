apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi # This must be less than or equal to the PV's capacity
  storageClassName: "" # This is critical for manual binding
